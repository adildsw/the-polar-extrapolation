export interface IIceArea {
    Year: string,
    Region: string,
    Extent: string,
    Area: string
}

export interface IIceAge {
    Year: number,
    "Avg percentage": number,
    "ice age": number
}